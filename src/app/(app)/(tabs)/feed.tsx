import { Text, View } from 'react-native';

const FeedScreen = () => {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-foreground">Feed</Text>
    </View>
  );
};

export default FeedScreen;
